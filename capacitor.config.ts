import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ekarigar.kaamchoremp',
  appName: 'kaamChorEmp',
  webDir: 'www',
  plugins: {
    EdgeToEdge: {
      backgroundColor: "#511168",
    },
  },
};

export default config;
